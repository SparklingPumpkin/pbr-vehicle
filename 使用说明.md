# PBR Vehicle 使用说明

`pbr-vehicle` 是普通 Gaussian 车辆的 PBR 化、人工调节和自动外观适配交付集合。当前包含三个可独立安装的子包，以及一个可直接使用的 PBR 车辆资产库。

## 目录与版本

```text
pbr-vehicle/
  Assets/
    pbr_assets/                 # 已转换的 PBR 车辆资产库
  pbr-vehicle-convert/          # 普通车辆 Gaussian -> PBR 资产
  pbr-vehicle-panel/            # 手动加载与调节面板
  pbr-vehicle-auto/             # 自动外观适配
```

| 子包 | 当前版本 | 作用 | 主要命令 |
| --- | --- | --- | --- |
| `pbr-vehicle-convert` | `pbr-vehicle-sdk 1.4.0` | 将标准 3DGS 车辆 PLY 转换为 PBR 资产。 | `pbr-vehicle` |
| `pbr-vehicle-panel` | `pbr-vehicle-panel 1.2.2` | 使用 Viser 加载场景和 PBR 车辆，手动调节姿态、材质、光照与投影。 | `pbr-vehicle-panel` |
| `pbr-vehicle-auto` | `pbr-vehicle-auto 1.1.1` | 以普通场景 Gaussian 的 DC/opacity 为目标，自动适配车辆外观。 | `pbr-vehicle-auto-fit` |

## 资产库

`Assets/pbr_assets/` 是 PBR 车辆资产库，不是场景库。当前包含已验收的 `10010` 至 `10015` 六辆车；每一辆车均可直接作为 Panel 和 Auto 的 `--asset-dir` / `--vehicle-asset-folder` 输入。

每辆车的 canonical 配置为：

```text
Assets/pbr_assets/<asset-id>/configs/config_<asset-id>.json
```

该文件用于发现 `original`、`proxy` 和 `mapping` 三个二进制 payload。不要根据文件名自行推断路径，也不要将场景或自动配置替代 canonical 配置。完整格式、字段与校验规则见 [Assets/PBR资产格式说明.md](Assets/PBR资产格式说明.md)。

## 完整流程

```text
标准车辆 3DGS PLY
  -> Convert: 生成 PBR 车辆资产
  -> Panel: 手动确认姿态、太阳方向、材质与投影
  -> Auto: 使用场景 DC/opacity 自动适配亮度和环境色温
  -> Panel / 原生渲染器: 加载最终配置进行预览或渲染
```

三段之间通过资产目录中的 canonical 配置和场景/自动配置 JSON 传递状态：

- Convert 写入 canonical `config_<asset-id>.json`。
- Panel 在同一资产的 `configs/` 下保存手动场景配置。
- Auto 读取 canonical 配置或完整 Panel 状态模板，并在同一 `configs/` 下写入 `config_auto_<asset>_<scene>...json`。

自动配置是候选结果。仅当 `auto_fit.status` 为 `candidate_only` 且指标有改善时才会写入拟合参数；`rejected_no_improvement` 表示保持模板/基础外观，不应将其解释为成功调参。

## 安装

三个子包可分别安装。以下命令假定当前目录为本部署根目录：

```bash
python -m pip install ./pbr-vehicle-convert/package
python -m pip install ./pbr-vehicle-panel
python -m pip install ./pbr-vehicle-auto
```

Panel 如果需要直接读取 DriveStudio `.pth` 场景检查点，再安装其可选依赖：

```bash
python -m pip install './pbr-vehicle-panel[pth]'
```

也可分别在各子包根目录使用 `python -m pip install -e .` 进行开发安装。各包 `dist/` 目录内的 `SHA256SUMS` 对应当前 wheel 的完整性校验。

## 1. 转换普通车辆

输入必须是标准 3DGS 车辆 PLY，至少包含 `x/y/z`、`f_dc_0..2`、`scale_0..2`、`rot_0..3` 和 `opacity`。正式 P-v3 路径使用 `complete-asset`：

```bash
pbr-vehicle complete-asset \
  path/to/vehicle.ply \
  Assets/pbr_assets/vehicle_new \
  --asset-id vehicle_new \
  --config pbr-vehicle-convert/package/examples/configs/config_default.json

pbr-vehicle inspect-complete Assets/pbr_assets/vehicle_new
```

它保留源车辆每个 Gaussian 的中心和 opacity，并生成直接白模 2DGS proxy 与 identity mapping。`convert` 入口仍支持一般 KNN proxy；其输出同样会写 v4 canonical 配置，Panel 与 Auto 均可读取。转换详情见 [pbr-vehicle-convert/使用指南.md](pbr-vehicle-convert/使用指南.md)。

## 2. 手动调节

Panel 可加载普通 Gaussian `.ply` 场景或 DriveStudio `.pth` 场景，也可只加载车辆：

```bash
pbr-vehicle-panel \
  --scene Scenes/scene.ply \
  --vehicle-asset-folder Assets/pbr_assets/10010 \
  --config Assets/pbr_assets/10010/configs/config_10010.json \
  --port 18091
```

浏览器打开 `http://localhost:18091/`。面板直接提供太阳强度、方位角、高度角、车辆色温、Albedo 饱和度、补光和 Center orbit；完整参数集中在 `高级 > Transform / Material / R3GW Lighting / Projection`。保存后的完整 Viser 状态可作为 Auto 的模板。

Panel 是交互预览和编辑器，并不承诺与 DriveStudio 原生 rasterizer 的逐像素一致性。详细参数见 [pbr-vehicle-panel/使用指南.md](pbr-vehicle-panel/使用指南.md)。

## 3. 自动外观适配

Auto 的场景输入是普通 Gaussian PLY，只使用 `f_dc_0..2` 与 `opacity`。场景不需要是 PBR 场景，也不使用截图、2D RGB、车辆掩码或场景 PBR 字段。

```bash
pbr-vehicle-auto-fit \
  --scene-ply Scenes/argoverse004/background.ply \
  --asset-dir Assets/pbr_assets/10010 \
  --template-config pbr-vehicle-auto/src/pbr_vehicle_auto/templates/default_vehicle_pbr_viser_template.json \
  --output-dir runs/argoverse004-10010-auto \
  --final-config Assets/pbr_assets/10010/configs/config_auto_10010_argoverse004.json \
  --max-seconds 120
```

Auto 保留模板中的车辆姿态、投影、材质、太阳方向和太阳 RGB；仅搜索 `sun_intensity`、`ambient_fill` 与 `environment_cct_kelvin`。环境色温限制在 `3500-7200 K`、能量固定，车辆饱和度是手动参数而非拟合变量。

输出目录包含：

- `metrics.json`：原始与烘焙后的 opacity 加权亮度 CDF-L1 指标及阶段耗时；
- `candidates.json`：所有已评估候选；
- `*_rear_view_cct_pbr_dc_baked.ply`：后视角 PBR 结果写回普通 Gaussian DC 的诊断产物；
- `--final-config` 指定的配置：供 Panel 或下游渲染加载。

Auto 只建立单个车辆后视角的 DC 外观适配，不恢复物理真实光照、不估计太阳方向、不恢复材质真值，也不负责地面投影方位。

## 配置选择

- 仅查看或手动编辑新资产：使用 canonical `config_<asset-id>.json`。
- 复现某次 Panel 手调：使用该次 Panel 保存的 `config_*.json`。
- 使用 Auto：优先传入完整 Panel 状态模板；没有模板时可使用包内默认模板，但应确认其资产 ID、姿态和手动太阳参数适用于当前任务。
- 加载 Auto 结果：检查 `auto_fit.status` 和 `auto_fit.metric`，再把该 JSON 作为 Panel 的 `--config` 或下游渲染配置。

## 边界

资产转换、Panel 和 Auto 的共同合同是 `pbr-vehicle-config-folder-v4`。资产的最终可见层始终是 `original` Gaussian；`proxy` 只负责承载 PBR 法线、反照率与光照计算。运行时投影是 `receiver_space_mask_v1`，不会在资产目录中保存静态 `projection_*.ply`。

## 发布到 GitHub

共享 Linux 账号不需要保存 GitHub 私钥。运行以下脚本后，在你自己的电脑浏览器完成 GitHub CLI 显示的一次性设备授权即可：

```bash
bash scripts/publish_github.sh
```

脚本会先检查服务器是否能连接 GitHub，随后在远程终端显示一次性代码，并要求在本地浏览器打开 `https://github.com/login/device` 输入该代码；它不会尝试在服务器上启动浏览器。若设备码申请遇到瞬时网络超时，脚本会自动重试三次；若仍失败，则必须先为该 SSH 终端配置可用的 HTTPS 出网路径，因为服务器仍需向 GitHub 上传提交。脚本只使用临时 GitHub 配置目录，不会写入 `~/.config/gh` 或全局 Git 凭据；完成推送后会自动注销并清除临时授权。它发布到 `https://github.com/SparklingPumpkin/pbr-vehicle`，并默认忽略缓存、wheel、构建目录和 Auto 验证产物。资产库会保留。推送前脚本会拒绝任何超过 `95 MiB` 的待提交文件。

如果服务器无直接 GitHub 出网，但你的本地电脑可以访问 GitHub，可在本地电脑另开一个 SSH 终端，建立临时反向 SOCKS 通道：

```bash
ssh -N -R 127.0.0.1:17891 <server-login>
```

保持该本地 SSH 终端运行。在服务器的另一终端执行：

```bash
cd /mnt/fjn/projects/fjn/pbr-vehicle
PBR_VEHICLE_PROXY=socks5://127.0.0.1:17891 ./scripts/publish_github.sh
```

其中 `<server-login>` 使用你平时连接该服务器的 SSH 地址，例如 `root@server.example.com`。`17891` 是临时端口，可换成未占用的端口。这里必须使用 `socks5://` 而不是 `socks5h://`，因为当前 GitHub CLI 仅支持前者。该隧道只监听服务器的 `127.0.0.1`，断开本地 SSH 后立即失效；脚本不会把该代理地址或 GitHub 授权写入全局配置。不要使用 `17890`，它属于 Codex 的受认证代理，普通 shell 会收到 `407 Proxy Authentication Required`。
