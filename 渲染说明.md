# PBR 车辆面板渲染原理与代码对照

## 1. 文档范围

本文档说明 `pbr-vehicle-panel` 交付包的实时预览渲染流程，并逐项列出它与普通 3D Gaussian Splatting（3DGS）渲染的不同。对应代码主要位于：

- `pbr-vehicle-panel/src/pbr_vehicle_standalone/asset_io.py`：场景、车辆和 PBR 属性加载；
- `pbr-vehicle-panel/src/pbr_vehicle_standalone/shading.py`：车辆光照和材质计算；
- `pbr-vehicle-panel/src/pbr_vehicle_standalone/projection.py`：车底 core 和太阳 cast extension 两层投影；
- `pbr-vehicle-panel/src/pbr_vehicle_standalone/rendering.py`：Gaussian buffer 和投影 glTF 平面封装；
- `pbr-vehicle-panel/src/pbr_vehicle_standalone/viewer.py`：面板参数映射、坐标变换与 Viser 场景提交。

> 重要：这是交互预览实现。它在 CPU/NumPy 上先为每个车辆 Gaussian 计算一个 RGB，然后交给 Viser 做标准 Gaussian splat 合成。它不是 DriveStudio CUDA rasterizer 中的逐像素、逐视角 GGX 积分，不保证与原生训练/渲染器像素一致。

## 2. 总体数据流

```text
普通场景 PLY / PTH Background
        └── 保留普通 Gaussian 颜色、不参与车辆 PBR

车辆资产目录
        ├── original PLY  ── 高细节几何与原始外观
        ├── proxy PLY     ── 法线、Albedo、粗糙度、金属度
        ├── map NPZ       ── original 到 proxy 的 KNN/单点映射
        └── config JSON   ── 材质、光照、变换和投影参数
                    │
                    ▼
          proxy 上计算近似 PBR RGB
                    │
                    ▼
          计算重光照比率并映射到 original
                    │
                    ├── original Gaussian 交给 Viser
                    └── core/cast RGBA 平面交给 Viser
```

默认显示模式为 `Relight Original`：proxy 承担光照计算，original 承担最终可见几何和细节。

## 3. 普通 Gaussian 渲染基线

一个普通 3D Gaussian 可表示为：

$$
G_i = (\boldsymbol\mu_i,\,\boldsymbol\Sigma_i,\,\alpha_i,\,\mathbf c_i),
$$

其中 $\boldsymbol\mu_i$ 是中心，$\boldsymbol\Sigma_i$ 是 3D 协方差，$\alpha_i$ 是不透明度，$\mathbf c_i$ 是由 SH 或已解码 RGB 得到的颜色。车辆和场景 PLY 中的参数解码为：

$$
\alpha_i=\sigma(o_i),\qquad
\mathbf s_i=\exp(\hat{\mathbf s}_i),\qquad
\boldsymbol\Sigma_i=\mathbf R(\mathbf q_i)\,\operatorname{diag}(\mathbf s_i^2)\,\mathbf R(\mathbf q_i)^T.
$$

PLY 的 DC 颜色解码为：

$$
\mathbf c_i=\operatorname{clip}(C_0\mathbf f_{dc,i}+0.5,0,1),
\qquad C_0=0.28209479177387814.
$$

Viser 将 3D Gaussian 投影为屏幕空间椭圆，并按深度使用 front-to-back alpha 合成：

$$
\mathbf C(p)=\sum_i T_i(p)\,a_i(p)\,\mathbf c_i,
\qquad
T_i(p)=\prod_{j<i}\left(1-a_j(p)\right),
$$

$$
a_i(p)=\alpha_i\exp\left[-\frac12
(p-\boldsymbol\mu'_i)^T\boldsymbol\Sigma_i'^{-1}(p-\boldsymbol\mu'_i)\right].
$$

交付包沿用了这一几何投影和 alpha 合成过程，但它只读取 `f_dc_0..2` 或 `red/green/blue`，不读取和评估 `f_rest_*` 高阶 SH。因此即使 PLY 含高阶 SH，本面板中的普通场景和 `Original SH` 模式也是 DC-only，没有标准 3DGS 的视角相关颜色。另一个核心区别是：场景 $\mathbf c_i$ 保持静态，但车辆 $\mathbf c_i$ 会在每次面板参数变化后由 PBR 属性重新计算。

## 4. 车辆资产比普通 Gaussian 多了什么

### 4.1 双层 Gaussian

普通 3DGS 只需要一组 Gaussian。本方案使用：

- `original` 层：保留最终展示所需的高细节 Gaussian；
- `proxy` 层：保存较稳定的 PBR 属性，用于计算材质和光照；
- `mapping`：将 proxy 上的重光照结果传回 original。

所有 Gaussian 都加载，没有数量上限或抽样步骤。

### 4.2 新增的逐 Gaussian 属性

除了中心、尺度、旋转、opacity 和 SH/RGB，proxy 还包含：

$$
(\mathbf n_i,\,\mathbf a_i,\,r_i,\,m_i),
$$

即法线、Albedo、粗糙度和金属度。字段同时兼容 `r3gw_*` 和旧的非前缀命名。

如果缺少显式法线，代码取协方差最短主轴作为表面法线：

$$
\mathbf n_i=\mathbf R(\mathbf q_i)[:,\arg\min_k s_{ik}],
$$

再依据车辆中心到 Gaussian 中心的方向翻转法线，使其大致朝外。

### 4.3 材质面板的作用方式

使用资产材质时，面板的粗糙度以 `0.40` 为中性偏移，金属度作为增量：

$$
r_i'=\operatorname{clip}(r_i+r_{panel}-0.40,0.02,0.98),
$$

$$
m_i'=\operatorname{clip}(m_i+m_{panel},0,1).
$$

关闭“Use asset material”时，全部 Gaussian 直接使用面板的统一 $r_{panel}$ 和 $m_{panel}$。

## 5. 颜色、饱和度与色温

### 5.1 Albedo 饱和度

先按 Rec.709 亮度系数计算灰度：

$$
Y=0.2126R+0.7152G+0.0722B.
$$

再用饱和度倍率 $s$ 调整颜色：

$$
\mathbf a'=\operatorname{clip}\left(Y+s(\mathbf a-Y),0,1\right).
$$

$s=0$ 是灰度，$s=1$ 保留原颜色，$s=2$ 增强饱和度。面板显示值 $u_s\in[-1,1]$ 映射为：

$$
s=1+u_s.
$$

因此界面 `0` 等于原渲染参数 `1.0`。

### 5.2 环境光色温

色温通过黑体颜色近似函数转为 RGB gain，并以 $6500\,K$ 的输出归一化：

$$
\mathbf g_T=\operatorname{clip}\left(
\frac{\operatorname{BlackBodyRGB}(T)}{\operatorname{BlackBodyRGB}(6500)},0,3
\right).
$$

界面值 $u_T\in[-1,1]$ 按原有 `2000~12000 K` 范围分段映射：

$$
T(u_T)=
\begin{cases}
6500+4500u_T,&u_T<0,\\
6500+5500u_T,&u_T\ge 0.
\end{cases}
$$

即 `-1 → 2000 K`、`0 → 6500 K`、`1 → 12000 K`。环境光 RGB 由色温派生；太阳光 RGB 保持独立，不从环境色温派生。

## 6. 环境光

环境光使用前 9 个二阶实 SH 基函数：

$$
\mathbf E(\mathbf n)=\max\left(
\sum_{k=0}^{8}Y_k(\mathbf n)\,\mathbf L_k',0
\right),
$$

$$
\mathbf L_k'=I_{env}\,\mathbf g_T\odot\mathbf L_k.
$$

`shade_vehicle` 函数接口允许传入二阶环境 SH。但是当前独立 `StandaloneViewer` 的调用没有传入 `env_sh`，因此实际运行时使用只有 $L_0=1/C_0$ 的均匀白色环境。环境光的方向差异式公式是可用能力，不是当前独立面板的默认输入。

材质中的“亮度”实际是环境补光倍率 $f$：

$$
\mathbf L_{ambient}=f\,\mathbf E(\mathbf n).
$$

界面值 $u_f\in[-1,1]$ 映射到原范围 `0~1`，且 `0` 对应原默认 `0.35`：

$$
f(u_f)=
\begin{cases}
0.35+0.35u_f,&u_f<0,\\
0.35+0.65u_f,&u_f\ge0.
\end{cases}
$$

## 7. 方向太阳光

方位角 $a$ 和高度角 $e$ 保持度数控制，光照方向为：

$$
\mathbf l=
\begin{bmatrix}
\cos e\cos a\\
\cos e\sin a\\
\sin e
\end{bmatrix},
\qquad
n\!\cdot\!l=\max(\mathbf n\cdot\mathbf l,0).
$$

太阳直接光与环境光相加：

$$
\mathbf L_{direct}=
\mathbf c_{sun}\,I_{sun}\,V\,(n\!\cdot\!l)
+\mathbf L_{ambient},
$$

其中 $V\in[0,1]$ 是 visibility scale。太阳强度界面值 $u_I\in[-1,1]$ 映射到原范围 `0~8`，且 `0` 对应 `1.0`：

$$
I_{sun}(u_I)=
\begin{cases}
1+u_I,&u_I<0,\\
1+7u_I,&u_I\ge0.
\end{cases}
$$

车辆旋转时，太阳光保持在世界坐标系。若车辆旋转矩阵为 $\mathbf R_v$，则 PBR 和投影在车辆局部坐标中使用：

$$
\mathbf l_{local}=\mathbf R_v^T\mathbf l_{world}.
$$

这一点与普通 SH Gaussian 的静态颜色明显不同：改变太阳角度会重算车辆明暗和 cast extension，但不改变普通场景。

## 8. 交互预览的近似 PBR BRDF

代码使用一个固定预览视线：

$$
\mathbf v=\operatorname{normalize}(0,-1,0.6),
$$

并用 Blinn 半程向量近似镜面方向：

$$
\mathbf h=\operatorname{normalize}(\mathbf l+\mathbf v),
\qquad q=\max(\mathbf n\cdot\mathbf h,0).
$$

基础反射率在非金属 reflectance $F_d$ 和金属 Albedo 之间插值：

$$
\mathbf F_0=F_d(1-m)+\mathbf a'm.
$$

漫反射项：

$$
\mathbf C_{diffuse}=\mathbf a'\odot\mathbf L_{direct}(1-m).
$$

令

$$
g=(1-r)^2,
$$

预览实现用两个幂函数合成高光：

$$
B=q^{1+3g},\qquad T=q^{4+40g},
$$

$$
\mathbf C_{specular}=
\mathbf L_{direct}\odot\mathbf F_0
(0.45+14g)(0.85B+2.5T).
$$

最终 proxy 颜色为：

$$
\mathbf C_{proxy}=\operatorname{clip}\left[
E_x(\mathbf C_{diffuse}+\mathbf C_{specular}),0,1
\right],
$$

其中 $E_x$ 是 exposure。

这里与严格微表面 GGX 的不同必须明确：

- 没有显式的 GGX normal distribution $D$、Smith geometry $G$ 和 Schlick Fresnel $F$；
- 没有对每个屏幕像素使用真实相机视线，而是使用固定 $\mathbf v$；
- 没有环境镜面预滤波、BRDF LUT、色调映射或 HDR 输出；
- 颜色在提交 Viser 前被截断到 `[0,1]` 并量化为 8-bit RGB。

因此它的目标是面板交互中稳定、快速地表达材质和光照变化，而不是作为离线物理标准渲染器。

## 9. Proxy 到 Original 的重光照映射

默认 `Relight Original` 不直接显示 proxy。先在 proxy 上计算相对于 proxy Albedo 的逐通道光照比率：

$$
\boldsymbol\rho_j=
\frac{\mathbf C_{proxy,j}}
{\max(\mathbf a_j,0.03)}.
$$

对于 original Gaussian $i$，用 NPZ 中的 KNN 索引 $M_{ik}$ 和归一化权重 $w_{ik}$ 映射：

$$
\hat{\boldsymbol\rho}_i=
\sum_k w_{ik}\boldsymbol\rho_{M_{ik}},
\qquad
\sum_k w_{ik}=1.
$$

P-v3 identity/direct-white 映射是 $K=1,w=1$ 的特例。然后用重光照强度 $\lambda$ 从原外观到完整重光照做插值：

$$
\tilde{\boldsymbol\rho}_i=1+\lambda(\hat{\boldsymbol\rho}_i-1),
$$

$$
\mathbf C_{original,i}'=
\operatorname{clip}\left(
\operatorname{Saturation}(\mathbf C_{original,i},s)
\odot\tilde{\boldsymbol\rho}_i,0,1
\right).
$$

与普通 Gaussian 相比，这是本方案的一个关键差异：它不仅是修改 original 的固定 RGB，而是在保留 original 细节的同时，传递 proxy 上计算的相对光照变化。

## 10. 变换与协方差

车辆的缩放 $s_v$、旋转 $\mathbf R_v$ 和平移 $\mathbf t_v$ 作用为：

$$
\boldsymbol\mu_i'=s_v\boldsymbol\mu_i,
\qquad
\boldsymbol\Sigma_i'=s_v^2\boldsymbol\Sigma_i.
$$

局部缩放后的 Gaussian buffer 与车辆节点的 $\mathbf R_v,\mathbf t_v$ 一起提交给 Viser。协方差必须乘 $s_v^2$，而不是 $s_v$，这是保持 Gaussian 几何尺寸正确的必要条件。

## 11. 两层车辆投影

普通 3DGS 本身不会在未建模的地面上自动产生可控阴影。本方案额外生成两张程序化 RGBA mask，作为透明 glTF 平面叠加到地面。它们不是 Gaussian PLY。

### 11.1 固定车底 Contact Core

地面高度由 proxy Gaussian 底面估计：

$$
b_i=z_i-\sigma_a\sqrt{\Sigma_{i,zz}},
$$

$$
z_g=\operatorname{Percentile}_{p}(\{b_i\})+z_{offset}.
$$

资产包有固定 ground anchor 时，默认直接复用它；只有底面 percentile 或 sigma 被改动时才重算。因此 core 不随太阳角度移动。

core 的平面形状是根据车辆 XY 的 `1%~99%` 分位包围盒构建的圆角矩形，可调长宽、XY 偏移、圆角、边缘柔化、opacity 和灰度 brightness。

令 $d_c(x,y)$ 为像素到圆角矩形的符号距离，则 core mask 可概括为：

$$
M_c=\operatorname{Blur}(\mathbf 1[d_c\le0],\sigma_c),
$$

$$
\alpha_c=o_c M_c,\qquad \mathbf C_c=(b_c,b_c,b_c).
$$

### 11.2 随太阳变化的 Cast Extension

对每个 proxy 中心 $\mathbf p_i=(x_i,y_i,z_i)$，沿局部太阳方向 $\mathbf l=(l_x,l_y,l_z)$ 投到 $z=z_g$ 平面：

$$
h_i=\max(z_i-z_g,0),
$$

$$
\mathbf p_i^{cast}=\mathbf p_i-\frac{h_i}{l_z}\mathbf l.
$$

对 $\mathbf p_i^{cast}$ 的 XY 坐标求凸包，得到随方位角和高度角变化的车辆外轮廓。当 $l_z<\sin 1^\circ$ 时不生成投影，避免除以近乎零的数值不稳定。

对平面像素到 core 边界的非负距离 $d$ 做归一化：

$$
t=\operatorname{clip}\left(\frac{d}{D},0,1\right).
$$

随距离的模糊混合为：

$$
u_b=t^{\gamma_b},
$$

$$
M_e=M_{base}(1-u_b)+M_{far}u_b.
$$

其中 $M_{base}$ 用基础边缘柔化生成，$M_{far}$ 使用增长后的柔化尺度。opacity 距离衰减为：

$$
F_o=\operatorname{clip}(1-k_ot^{\gamma_o},0,1),
$$

$$
\alpha_e=o_eM_eF_o.
$$

投影灰度向白色的距离变化为：

$$
b_e(t)=b_0+(1-b_0)k_bt^{\gamma_c}.
$$

因此该“投影”是色值可调、边缘可模糊、opacity 可衰减的程序化接收面，不是光线追踪可见性或 Gaussian 自阴影。

### 11.3 两层合成

extension 和 core 使用两个独立 glTF `alphaMode=BLEND` 平面，两者都是完整 mask，不从 extension 中扣除 core，因此不会为 core 预留透明方形空洞。

两层分别加上 `0.001` 和 `0.002` 的局部 Z bias，减少平面深度冲突。全局投影 opacity 与 visibility 还会乘到两层 alpha 上：

$$
\alpha_{final}=\alpha_{layer}\,o_{global}\,V.
$$

## 12. Viser 提交和量化

车辆颜色计算完成后，每个 Gaussian 封装为 32-byte buffer：

```text
center float32 x 3
padding uint8 x 4
covariance upper triangle float16 x 6
RGB uint8 x 3
opacity uint8 x 1
```

这意味着：

- 几何仍以 Gaussian 中心和协方差表示；
- PBR 只决定提交时的 RGB，不改变 Gaussian 形状和 opacity；
- RGB 和 opacity 在网络/前端 buffer 中量化为 `uint8`；
- 投影不进入 Gaussian buffer，而是单独的 RGBA 纹理 glTF 平面。

## 13. 场景 PLY/PTH 的处理

场景与车辆的逻辑是隔离的：

- PLY 场景直接解码并作为普通 Gaussian 显示，但当前加载器只使用 DC/RGB，忽略高阶 `f_rest_*`；
- PTH 场景只提取 checkpoint 中的 `Background` 模型，生成缓存 PLY；
- PTH 转换保留 means、opacity、DC feature、scale 和 quaternion；
- 交付包的 PTH 转换不写出高阶 SH，因此转换后场景在面板中是 DC 颜色预览；
- 场景不需要 R3GW/PBR 属性，也不参与车辆光照重算。

因此“PTH 场景不带 R3GW 参数”不会禁用车辆 PBR：PBR 属性来自车辆 proxy，光照只作用于车辆。

## 14. 显示模式

| 模式 | 几何层 | 颜色来源 | 是否 PBR |
| --- | --- | --- | --- |
| `Relight Original` | original | original 颜色 × proxy 映射的重光照比率 | 是，默认 |
| `Proxy Lit` | proxy | proxy 近似 PBR 计算 | 是 |
| `Original SH` | original | original DC/RGB | 否 |
| `Albedo` | proxy | 饱和度调整后的 Albedo | 调试通道 |
| `Normal` | proxy | $(\mathbf n+1)/2$ | 调试通道 |
| `Roughness` | proxy | 粗糙度复制到 RGB | 调试通道 |
| `Reflectance` | proxy | 统一 reflectance | 调试通道 |
| `Metallic` | proxy | 金属度复制到 RGB | 调试通道 |
| `SH RGB` | proxy | proxy 原始 DC/RGB | 否 |

## 15. 与普通 Gaussian 渲染的差异汇总

| 环节 | 普通 3DGS | 本方案 |
| --- | --- | --- |
| 资产 | 一个 PLY/checkpoint | original PLY + proxy PLY + mapping NPZ + config JSON |
| 逐点属性 | 中心、协方差、opacity、SH | 额外加载法线、Albedo、粗糙度、金属度 |
| 颜色来源 | 完整 SH 可随视角评估 | 加载器只解码 DC/RGB；车辆颜色再由材质、环境光和太阳光动态重算 |
| 光照 | 通常烘焙在 SH 外观中 | 环境 SH + 独立方向太阳，支持逐车/共享 |
| 材质 | 无显式材质 | 可调粗糙度、金属度、reflectance、exposure、饱和度、重光照强度 |
| 高细节重光照 | 无 | proxy 计算光照比率，通过 KNN/identity mapping 传给 original |
| 太阳角度 | 不会改变已训练颜色 | 会改变车辆明暗、高光和 cast extension |
| 投影 | 不包含程序化接收面阴影 | 固定 core + 随太阳变化的 extension，两层 alpha 叠加 |
| 场景 | 普通 Gaussian | 仍然是普通 Gaussian，不进行 PBR |
| Gaussian 合成 | splat + alpha compositing | 完全沿用 Viser 的 splat + alpha compositing |
| 颜色精度 | 取决于 rasterizer | 提交前 clamp 到 `[0,1]`，Viser buffer 中为 8-bit RGB |
| 性能模式 | 可在 GPU shader/rasterizer 中计算 | 参数变更时在 CPU/NumPy 上重算车辆 RGB 和投影 mask |

## 16. 实现边界

使用该渲染结果时需要保留以下边界：

- PBR 只作用于车辆，不会反向改变场景颜色；
- 车辆不会投射真实光线追踪阴影到场景 Gaussian，场景也不会对车辆产生几何遮挡阴影；
- 固定视线意味着镜面高光不会严格跟随 Viser 相机移动；
- 当前独立 Viewer 实际使用均匀 L0 环境；二阶 SH 评估代码已存在但调用端未传入环境 SH，它也不是完整 HDR environment map；
- PTH 场景转换为 DC-only 预览，不保留高阶 SH；
- 投影是程序化平面 mask，不是光照方程中的可见性积分；
- 多车可共享场景光照，也可以关闭共享后使用各自独立的光照状态。

总结来说，本交付包在不改变 Viser 普通 Gaussian 几何投影和 alpha 合成的前提下，新增了车辆专用的 PBR 属性、动态预着色、proxy-to-original 重光照映射和两层程序化投影。它适合用于快速布置、调参和视觉预览，不应被当作完整离线物理渲染器的替代品。
